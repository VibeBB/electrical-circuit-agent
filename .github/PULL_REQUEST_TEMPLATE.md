## Summary

<!-- What does this change and why? -->

## Verification

<!-- Paste commands run and their results. -->

- [ ] `uv run python scripts/verify_all.py --stage docs` passes
- [ ] `uv run python scripts/verify_all.py --stage fast` passes
- [ ] PR title, description, and commit messages are written in English
- [ ] No runtime behavior change in `src/`, `scripts/`, `tests/`, or plugin
      runtime assets — OR the change is intentional and explained above
- [ ] ADR added or updated in `docs/adr/` if this changes an accepted decision

## Notes for reviewers

